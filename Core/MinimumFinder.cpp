#include "MinimumFinder.h"

GNEB::MinimumFinder::MinimumFinder()
{
}

std::deque<Point> GNEB::MinimumFinder::findMinima()
{
	std::deque<std::deque<Point *>>::iterator it = Plane::getInstance()->getPoints().begin();
	
	std::deque<Point> x;

	return x;
}
